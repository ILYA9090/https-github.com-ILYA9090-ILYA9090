import React from 'react'

const MySelect = ({option}) => {
  return (
    <div>
      <select>
          <option value="">{defaultValue}</option>
        </select>
    </div>
  )
}

export default MySelect
