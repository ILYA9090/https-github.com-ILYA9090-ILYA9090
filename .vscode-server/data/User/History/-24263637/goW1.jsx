import React from 'react'

const Select = ({defaultValue,defaultVal}) => {
  return (
    <select>
        <option value="">{defaultValue}</option>
        <option value="">{defaultVal}</option>
    </select>
      
  )
}

export default Select
