import React from 'react'

const Select = ({defaultVal}) => {
  return (
    <select>
        <option value="">{defaultValue}</option>
        <option value=""></option>
    </select>
      
  )
}

export default Select
